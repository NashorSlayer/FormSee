"use client"
import React, { useState } from 'react'
import { useDroppable } from '@dnd-kit/core'
import { arrayMove } from '@dnd-kit/sortable'


interface props {
    optionsList: string[]
}

const DroppableArea: React.FC<props> = ({ optionsList }) => {


    return (
        <div >

        </div>
    )
}

export default DroppableArea